logging:
  level:
    root: info

spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lemlistapp
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update

    database: mysql


  mail:
    host: sandbox.smtp.mailtrap.io
    username: e1b7e006fa41ee
    password: 7a9560a3f7ea2f
    port: 587
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true





url:
  ignore:
    swagger: /v2/api-docs, /webjars/*, /swagger-resources/, /actuator/health, /swagger-ui/index.html, /swagger-ui.html, /app//.{js,html}

springdoc:
  swagger-ui:
    path: /swagger
