spring:
  datasource:
    url: jdbc:mysql://localhost:3306/binaaz
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    user:
      name: user
      password: password

server:
  port: 8085